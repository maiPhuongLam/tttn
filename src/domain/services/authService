export interface IAuthService {
  register(data: ): Promise<User>
  login(data: LoginDto): Promise<IToken>
  logout(userId: number): Promise<boolean>
  refreshToken(userId: number, refreshToken: string, role: UserRoles): Promise<IToken>
}