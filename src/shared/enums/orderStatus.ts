export enum OrderStatusEnum {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SHIPED = 'shiped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  REUNFUNDED = 'refunded',
  RETURNED = 'returned',
}
