export enum OrderStatusEnum {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SHIPED = 'shiped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  COMPLETED = 'complete',
  REUNFUNDED = 'refunded',
  RETURNED = 'returned',
}
